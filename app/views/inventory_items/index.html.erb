	<%= content_for :header do %>
		<%= t(:inventory_item).pluralize %>
	<% end %>
	
	<%= content_for :toolbarbuttons do %>
		<%= link_to t(:inventory_item_new), new_binder_inventory_item_path(working_binder), :id => 'new_item_btn', :class => "btn btn-hb", :remote => true %>
	<% end %>
	
	<%= render :partial => 'shared/resource_index', :locals => { :resources => @inventory_items } %>
