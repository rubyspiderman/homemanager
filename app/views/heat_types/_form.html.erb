<%= form_for(@heat_type) do |f| %>
  <% if @heat_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@heat_type.errors.count, "error") %> prohibited this heat_type from being saved:</h2>

      <ul>
      <% @heat_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
