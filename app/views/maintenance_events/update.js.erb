<% if @maintenance_event.errors.any? %>
	
	showModalForm("<%= escape_javascript(render(:partial => "form")) %>");

<% else %>

	closeModalForm();
	updateDataTable(
		"<%= escape_javascript(render(:partial => "shared/resource_index", :locals => { :resources => @maintenance_events })) %>",
		"<%= @maintenance_events.count %>");

<% end %>