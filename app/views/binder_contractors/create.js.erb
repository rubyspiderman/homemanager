<% if @binder_contractor.errors.any? %>
	
	showModalForm("<%= escape_javascript(render(:partial => "form")) %>");

<% else %>

	closeModalForm();
	updateDataTable(
		"<%= escape_javascript(render(:partial => "shared/resource_index", :locals => { :resources => @binder_contractors })) %>",
		"<%= @binder_contractors.count %>");

<% end %>