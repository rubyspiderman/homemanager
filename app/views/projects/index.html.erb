	<%= content_for :header do %>
		<%= t(:project).pluralize %>
	<% end %>
	
	<%= content_for :toolbarbuttons do %>
		<%= link_to t(:project_new), new_binder_project_path(working_binder), :id => 'new_item_btn', :class => "btn btn-hb", :remote => true %>
	<% end %>
	
	<%= render :partial => 'shared/resource_index', :locals => { :resources => @projects } %>
