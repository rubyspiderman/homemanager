	<%= content_for :header do %>
		<%= t(:area).pluralize %>
	<% end %>
	
	<%= content_for :toolbarbuttons do %>
		<%= link_to t(:area_new), new_binder_area_path(working_binder), :id => 'new_item_btn', :class => "btn btn-hb", :remote => true %>
	<% end %>
	
	<%= render :partial => 'shared/resource_index', :locals => { :resources => @areas } %>
