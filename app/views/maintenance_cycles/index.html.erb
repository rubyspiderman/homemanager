<h1>Listing maintenance_cycles</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @maintenance_cycles.each do |maintenance_cycle| %>
  <tr>
    <td><%= link_to 'Show', maintenance_cycle %></td>
    <td><%= link_to 'Edit', edit_maintenance_cycle_path(maintenance_cycle) %></td>
    <td><%= link_to 'Destroy', maintenance_cycle, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Maintenance cycle', new_maintenance_cycle_path %>
